* {
  margin: 0;
  padding: 0;
}

a {
  text-decoration: none;
}

ul {
  list-style: none;
}

body {
  font-family: 'Arial';
  font-size: 10px;
  position: relative;
  overflow-x: hidden;
}

.container {
  max-width: 1300px;
  width: 90%;
  margin: 0 auto;
  position: relative;
}